import build from './bin/build';
import clean from './bin/clean';
import serve from './bin/preview';
export { build, clean, serve };
declare const _default: {
    build: typeof build;
    clean: typeof clean;
    serve: typeof serve;
};
export default _default;
